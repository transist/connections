if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p0@followers" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p0@followers"
else
  rvm --create  "ruby-1.9.2-p0@followers"
fi

if ! command -v bundle ; then
  gem install bundler
fi